{
  "buildCommand": "cd ../.. && pnpm --filter @data-room/shared build && pnpm --filter @data-room/web build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
